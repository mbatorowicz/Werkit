# Klucz używany do szyfrowania i odszyfrowywania tokenów dla aplikacji mobilnej Werkit. 
# Zmień go na długi losowy ciąg znaków w wersji produkcyjnej, np. generując go poleceniem `openssl rand -base64 32` w konsoli.
JWT_SECRET="TwojZabezpieczonyDowolnyKluczDlaAplikacjiMobilnej"

# Ciąg połączenia do bazy danych PostgreSQL (wersja dla Vercel Postgres / Neon)
# Pobierz go od dostawcy po utworzeniu bazy na produkcji.
POSTGRES_URL="postgres://default:password@ep-lively-water-123456.eu-central-1.aws.neon.tech:5432/verceldb?sslmode=require"
