import type { AppDictionary } from "@/i18n/types";

/** Wspólne etykiety magazynowe (materiały + DUR). */
export function warehouseCommonLabels(dict: AppDictionary) {
  return dict.common.warehouse;
}
